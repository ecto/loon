{
  "name": "loon-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsx build.ts --no-rust",
    "build:full": "tsx build.ts",
    "dev": "npm run build:full && cargo run -p loon-cli -- run web/dev.loon",
    "wasm": "wasm-pack build ../crates/loon-wasm --target web --out-dir ../../web/public"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
